# dsh-stock-plugin bundle patch.
# Applied when the plugin is installed via dsh plugin --profile web add dsh-stock-plugin.
# Registers the node-side plugin entry; the client half is picked up
# automatically from dsh.client in package.json by dsh-client-modules.
- insert:
    - id: dsh-stock-plugin
      name: dsh-stock-plugin
