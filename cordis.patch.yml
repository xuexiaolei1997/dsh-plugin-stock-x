# dsh-plugin-stock-x bundle patch.
# Applied when the plugin is installed via dsh plugin --profile web add dsh-plugin-stock-x.
# Registers the node-side plugin entry; the client half is picked up
# automatically from dsh.client in package.json by dsh-client-modules.
- insert:
    - id: dsh-plugin-stock-x
      name: dsh-plugin-stock-x
